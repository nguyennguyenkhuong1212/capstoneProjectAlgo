Use this block of code to run (no map plotting):

```
cd Code/solution
python solver.py ../data/location.csv false
```

Use this block of code to run (map plotting):

```
cd Code/solution
python solver.py ../data/location.csv true
```